# Google Gemini API key for the chat agent
GEMINI_API_KEY=AIza...

# GHL calendar embed URL (shown inline in chat when AI triggers booking)
# Get this from GHL → Calendars → Settings → Share → Embed Code
NEXT_PUBLIC_GHL_CALENDAR_URL=https://api.leadconnectorhq.com/widget/booking/YOUR_CALENDAR_ID

# Booking URL for navbar link (optional, can be same as above or a standalone page)
NEXT_PUBLIC_BOOKING_URL=https://api.leadconnectorhq.com/widget/booking/YOUR_CALENDAR_ID
